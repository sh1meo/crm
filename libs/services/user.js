class UserService {
  constructor() {}
  checkUserRole (userRole) {
    if (userRole === 'admin') {
        const userPermissions={
            read: true,
            write: true
        };
    } else if {
        userRole === 'seller'
    }
}
}

export default new UserService();